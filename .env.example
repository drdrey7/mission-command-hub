# Safe public example values only. Replace with your own backend details.

# Base URL for the Mission Command Hub backend API
VITE_OPENCLAW_API_URL=http://localhost:8780

# Optional bearer token for authenticated API requests
VITE_OPENCLAW_TOKEN=

# Frontend mock mode toggle for UI-only/demo work
VITE_USE_MOCK=false
